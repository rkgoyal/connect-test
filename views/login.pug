extends layout

block content
  main
    .container
      .row
        .col
        .col
          h4 <strong>Welcome to Alfred Health</strong>
          br
          form(method='POST', action='/user/login')
            if (message)
              .alert.alert-danger
                p <strong> Hmm.. </strong> Invalid username or password. Try again.
            #form-group
              label Username:
              input.form-control(name='username',type='string')
            #form-group
              label Password:
              input.form-control(name='password',type='password')
            br
            input.btn.btn-primary(type='submit',value='Sign In')
          br
          p Don't have an account?
          a(class='button', href='/user/register') Register here!
        .col
          

  
